name: Valctk Haber Bot (Every 39 Minutes)

on:
  schedule:
    - cron: "*/39 * * * *"   # her 39 dakikada bir
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    concurrency:
      group: valctk-haber-bot
      cancel-in-progress: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          cd ${{ github.workspace }}
          python3 -m pip install --upgrade pip
          pip install -r requirements.txt

      # İsteğe bağlı ama tavsiye: Secrets doğru mu hızlı kontrol
      - name: Preflight: verify X credentials (v2 + v1.1)
        env:
          TW_API_KEY: ${{ secrets.TW_API_KEY }}
          TW_API_SECRET: ${{ secrets.TW_API_SECRET }}
          TW_ACCESS_TOKEN: ${{ secrets.TW_ACCESS_TOKEN }}
          TW_ACCESS_SECRET: ${{ secrets.TW_ACCESS_SECRET }}
        run: |
          python3 - <<'PY'
          import os, sys, tweepy
          ck, cs = os.getenv("TW_API_KEY"), os.getenv("TW_API_SECRET")
          at, ase = os.getenv("TW_ACCESS_TOKEN"), os.getenv("TW_ACCESS_SECRET")
          miss=[k for k,v in {"TW_API_KEY":ck,"TW_API_SECRET":cs,"TW_ACCESS_TOKEN":at,"TW_ACCESS_SECRET":ase}.items() if not v]
          if miss:
              print("Missing secrets:", miss); sys.exit(1)
          # v2 (create_tweet) testi
          c = tweepy.Client(consumer_key=ck, consumer_secret=cs, access_token=at, access_token_secret=ase)
          me = c.get_me()
          print("v2 OK, user:", getattr(me.data, "username", None))
          # v1.1 (media upload) testi
          try:
            auth = tweepy.OAuth1UserHandler(ck, cs, at, ase)
            api = tweepy.API(auth)
            u = api.verify_credentials()
            print("v1.1 OK, screen_name:", getattr(u, "screen_name", None))
          except Exception as e:
            print("v1.1 WARN:", repr(e))  # medya başarısızsa bile bot çalışsın
          PY

      - name: Run bot
        env:
          TW_API_KEY: ${{ secrets.TW_API_KEY }}
          TW_API_SECRET: ${{ secrets.TW_API_SECRET }}
          TW_ACCESS_TOKEN: ${{ secrets.TW_ACCESS_TOKEN }}
          TW_ACCESS_SECRET: ${{ secrets.TW_ACCESS_SECRET }}
          DRY_MODE: "false"
          # Geçici: medya upload (v1.1) sıkıntı varsa kapat. Düzelince "true" yaparsın.
          ATTACH_OG_IMAGE: "false"
        run: |
          cd ${{ github.workspace }}
          ls -la
          python3 -u auto_repost_bot.py
